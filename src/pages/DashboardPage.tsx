import { DashboardContainer } from '@/containers/DashboardContainer';

/**
 * Dashboard Page
 * Main dashboard page that users see after login
 */
export const DashboardPage = () => {
    return <DashboardContainer />;
};

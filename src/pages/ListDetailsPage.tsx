import { ListDetailsContainer } from '@/containers/ListDetailsContainer';

/**
 * List Details Page
 * Page component for viewing tasks within a specific list
 */
export const ListDetailsPage = () => {
    return <ListDetailsContainer />;
};

import { ListsContainer } from '../containers/ListsContainer';

/**
 * Lists Page
 * Main page component for the My Lists feature
 */
export const ListsPage = () => {
    return <ListsContainer />;
};

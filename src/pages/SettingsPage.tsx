import { SettingsContainer } from '../containers/SettingsContainer';

/**
 * Settings Page
 * Main page component for the Account Settings feature
 */
export const SettingsPage = () => {
    return <SettingsContainer />;
};

import { TasksContainer } from '../containers/TasksContainer';

/**
 * Tasks Page
 * Main page component for the My Tasks feature
 */
export const TasksPage = () => {
    return <TasksContainer />;
};
